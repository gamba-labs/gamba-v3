/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { GAMBA_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const GAMBA_SET_AUTHORITY_DISCRIMINATOR = new Uint8Array([
  60, 11, 159, 59, 150, 12, 106, 78,
]);

export function getGambaSetAuthorityDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    GAMBA_SET_AUTHORITY_DISCRIMINATOR
  );
}

export type GambaSetAuthorityInstruction<
  TProgram extends string = typeof GAMBA_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountGambaState extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountGambaState extends string
        ? WritableAccount<TAccountGambaState>
        : TAccountGambaState,
      ...TRemainingAccounts,
    ]
  >;

export type GambaSetAuthorityInstructionData = {
  discriminator: ReadonlyUint8Array;
  authority: Address;
};

export type GambaSetAuthorityInstructionDataArgs = { authority: Address };

export function getGambaSetAuthorityInstructionDataEncoder(): FixedSizeEncoder<GambaSetAuthorityInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['authority', getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: GAMBA_SET_AUTHORITY_DISCRIMINATOR })
  );
}

export function getGambaSetAuthorityInstructionDataDecoder(): FixedSizeDecoder<GambaSetAuthorityInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['authority', getAddressDecoder()],
  ]);
}

export function getGambaSetAuthorityInstructionDataCodec(): FixedSizeCodec<
  GambaSetAuthorityInstructionDataArgs,
  GambaSetAuthorityInstructionData
> {
  return combineCodec(
    getGambaSetAuthorityInstructionDataEncoder(),
    getGambaSetAuthorityInstructionDataDecoder()
  );
}

export type GambaSetAuthorityAsyncInput<
  TAccountAuthority extends string = string,
  TAccountGambaState extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  gambaState?: Address<TAccountGambaState>;
  authorityArg: GambaSetAuthorityInstructionDataArgs['authority'];
};

export async function getGambaSetAuthorityInstructionAsync<
  TAccountAuthority extends string,
  TAccountGambaState extends string,
  TProgramAddress extends Address = typeof GAMBA_PROGRAM_ADDRESS,
>(
  input: GambaSetAuthorityAsyncInput<TAccountAuthority, TAccountGambaState>,
  config?: { programAddress?: TProgramAddress }
): Promise<
  GambaSetAuthorityInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountGambaState
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? GAMBA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    gambaState: { value: input.gambaState ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input, authority: input.authorityArg };

  // Resolve default values.
  if (!accounts.gambaState.value) {
    accounts.gambaState.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([34, 71, 65, 77, 66, 65, 95, 83, 84, 65, 84, 69, 34])
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.gambaState),
    ],
    programAddress,
    data: getGambaSetAuthorityInstructionDataEncoder().encode(
      args as GambaSetAuthorityInstructionDataArgs
    ),
  } as GambaSetAuthorityInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountGambaState
  >;

  return instruction;
}

export type GambaSetAuthorityInput<
  TAccountAuthority extends string = string,
  TAccountGambaState extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  gambaState: Address<TAccountGambaState>;
  authorityArg: GambaSetAuthorityInstructionDataArgs['authority'];
};

export function getGambaSetAuthorityInstruction<
  TAccountAuthority extends string,
  TAccountGambaState extends string,
  TProgramAddress extends Address = typeof GAMBA_PROGRAM_ADDRESS,
>(
  input: GambaSetAuthorityInput<TAccountAuthority, TAccountGambaState>,
  config?: { programAddress?: TProgramAddress }
): GambaSetAuthorityInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountGambaState
> {
  // Program address.
  const programAddress = config?.programAddress ?? GAMBA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    gambaState: { value: input.gambaState ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input, authority: input.authorityArg };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.gambaState),
    ],
    programAddress,
    data: getGambaSetAuthorityInstructionDataEncoder().encode(
      args as GambaSetAuthorityInstructionDataArgs
    ),
  } as GambaSetAuthorityInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountGambaState
  >;

  return instruction;
}

export type ParsedGambaSetAuthorityInstruction<
  TProgram extends string = typeof GAMBA_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    gambaState: TAccountMetas[1];
  };
  data: GambaSetAuthorityInstructionData;
};

export function parseGambaSetAuthorityInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedGambaSetAuthorityInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      gambaState: getNextAccount(),
    },
    data: getGambaSetAuthorityInstructionDataDecoder().decode(instruction.data),
  };
}
