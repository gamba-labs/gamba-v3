/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { MULTIPLAYER_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const SELECT_WINNERS_DISCRIMINATOR = new Uint8Array([
  80, 100, 28, 131, 83, 199, 222, 80,
]);

export function getSelectWinnersDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SELECT_WINNERS_DISCRIMINATOR
  );
}

export type SelectWinnersInstruction<
  TProgram extends string = typeof MULTIPLAYER_PROGRAM_ADDRESS,
  TAccountRng extends string | AccountMeta<string> = string,
  TAccountGambaState extends string | AccountMeta<string> = string,
  TAccountGameAccount extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountRng extends string
        ? WritableSignerAccount<TAccountRng> & AccountSignerMeta<TAccountRng>
        : TAccountRng,
      TAccountGambaState extends string
        ? ReadonlyAccount<TAccountGambaState>
        : TAccountGambaState,
      TAccountGameAccount extends string
        ? WritableAccount<TAccountGameAccount>
        : TAccountGameAccount,
      ...TRemainingAccounts,
    ]
  >;

export type SelectWinnersInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type SelectWinnersInstructionDataArgs = {};

export function getSelectWinnersInstructionDataEncoder(): FixedSizeEncoder<SelectWinnersInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: SELECT_WINNERS_DISCRIMINATOR })
  );
}

export function getSelectWinnersInstructionDataDecoder(): FixedSizeDecoder<SelectWinnersInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getSelectWinnersInstructionDataCodec(): FixedSizeCodec<
  SelectWinnersInstructionDataArgs,
  SelectWinnersInstructionData
> {
  return combineCodec(
    getSelectWinnersInstructionDataEncoder(),
    getSelectWinnersInstructionDataDecoder()
  );
}

export type SelectWinnersAsyncInput<
  TAccountRng extends string = string,
  TAccountGambaState extends string = string,
  TAccountGameAccount extends string = string,
> = {
  /** Authorised RNG bot */
  rng: TransactionSigner<TAccountRng>;
  /** Global config – only used to enforce `rng` */
  gambaState?: Address<TAccountGambaState>;
  /** Raw Game account; we parse fields by hand */
  gameAccount: Address<TAccountGameAccount>;
};

export async function getSelectWinnersInstructionAsync<
  TAccountRng extends string,
  TAccountGambaState extends string,
  TAccountGameAccount extends string,
  TProgramAddress extends Address = typeof MULTIPLAYER_PROGRAM_ADDRESS,
>(
  input: SelectWinnersAsyncInput<
    TAccountRng,
    TAccountGambaState,
    TAccountGameAccount
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  SelectWinnersInstruction<
    TProgramAddress,
    TAccountRng,
    TAccountGambaState,
    TAccountGameAccount
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? MULTIPLAYER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    rng: { value: input.rng ?? null, isWritable: true },
    gambaState: { value: input.gambaState ?? null, isWritable: false },
    gameAccount: { value: input.gameAccount ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.gambaState.value) {
    accounts.gambaState.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([71, 65, 77, 66, 65, 95, 83, 84, 65, 84, 69])
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.rng),
      getAccountMeta(accounts.gambaState),
      getAccountMeta(accounts.gameAccount),
    ],
    programAddress,
    data: getSelectWinnersInstructionDataEncoder().encode({}),
  } as SelectWinnersInstruction<
    TProgramAddress,
    TAccountRng,
    TAccountGambaState,
    TAccountGameAccount
  >;

  return instruction;
}

export type SelectWinnersInput<
  TAccountRng extends string = string,
  TAccountGambaState extends string = string,
  TAccountGameAccount extends string = string,
> = {
  /** Authorised RNG bot */
  rng: TransactionSigner<TAccountRng>;
  /** Global config – only used to enforce `rng` */
  gambaState: Address<TAccountGambaState>;
  /** Raw Game account; we parse fields by hand */
  gameAccount: Address<TAccountGameAccount>;
};

export function getSelectWinnersInstruction<
  TAccountRng extends string,
  TAccountGambaState extends string,
  TAccountGameAccount extends string,
  TProgramAddress extends Address = typeof MULTIPLAYER_PROGRAM_ADDRESS,
>(
  input: SelectWinnersInput<
    TAccountRng,
    TAccountGambaState,
    TAccountGameAccount
  >,
  config?: { programAddress?: TProgramAddress }
): SelectWinnersInstruction<
  TProgramAddress,
  TAccountRng,
  TAccountGambaState,
  TAccountGameAccount
> {
  // Program address.
  const programAddress = config?.programAddress ?? MULTIPLAYER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    rng: { value: input.rng ?? null, isWritable: true },
    gambaState: { value: input.gambaState ?? null, isWritable: false },
    gameAccount: { value: input.gameAccount ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.rng),
      getAccountMeta(accounts.gambaState),
      getAccountMeta(accounts.gameAccount),
    ],
    programAddress,
    data: getSelectWinnersInstructionDataEncoder().encode({}),
  } as SelectWinnersInstruction<
    TProgramAddress,
    TAccountRng,
    TAccountGambaState,
    TAccountGameAccount
  >;

  return instruction;
}

export type ParsedSelectWinnersInstruction<
  TProgram extends string = typeof MULTIPLAYER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Authorised RNG bot */
    rng: TAccountMetas[0];
    /** Global config – only used to enforce `rng` */
    gambaState: TAccountMetas[1];
    /** Raw Game account; we parse fields by hand */
    gameAccount: TAccountMetas[2];
  };
  data: SelectWinnersInstructionData;
};

export function parseSelectWinnersInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedSelectWinnersInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      rng: getNextAccount(),
      gambaState: getNextAccount(),
      gameAccount: getNextAccount(),
    },
    data: getSelectWinnersInstructionDataDecoder().decode(instruction.data),
  };
}
