/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { GAMBA_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const RNG_PROVIDE_HASHED_SEED_DISCRIMINATOR = new Uint8Array([
  238, 154, 25, 143, 191, 19, 25, 224,
]);

export function getRngProvideHashedSeedDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RNG_PROVIDE_HASHED_SEED_DISCRIMINATOR
  );
}

export type RngProvideHashedSeedInstruction<
  TProgram extends string = typeof GAMBA_PROGRAM_ADDRESS,
  TAccountGame extends string | AccountMeta<string> = string,
  TAccountRng extends string | AccountMeta<string> = string,
  TAccountGambaState extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountGame extends string
        ? WritableAccount<TAccountGame>
        : TAccountGame,
      TAccountRng extends string
        ? WritableSignerAccount<TAccountRng> & AccountSignerMeta<TAccountRng>
        : TAccountRng,
      TAccountGambaState extends string
        ? WritableAccount<TAccountGambaState>
        : TAccountGambaState,
      ...TRemainingAccounts,
    ]
  >;

export type RngProvideHashedSeedInstructionData = {
  discriminator: ReadonlyUint8Array;
  nextRngSeedHashed: string;
};

export type RngProvideHashedSeedInstructionDataArgs = {
  nextRngSeedHashed: string;
};

export function getRngProvideHashedSeedInstructionDataEncoder(): Encoder<RngProvideHashedSeedInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      [
        'nextRngSeedHashed',
        addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
      ],
    ]),
    (value) => ({
      ...value,
      discriminator: RNG_PROVIDE_HASHED_SEED_DISCRIMINATOR,
    })
  );
}

export function getRngProvideHashedSeedInstructionDataDecoder(): Decoder<RngProvideHashedSeedInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    [
      'nextRngSeedHashed',
      addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder()),
    ],
  ]);
}

export function getRngProvideHashedSeedInstructionDataCodec(): Codec<
  RngProvideHashedSeedInstructionDataArgs,
  RngProvideHashedSeedInstructionData
> {
  return combineCodec(
    getRngProvideHashedSeedInstructionDataEncoder(),
    getRngProvideHashedSeedInstructionDataDecoder()
  );
}

export type RngProvideHashedSeedAsyncInput<
  TAccountGame extends string = string,
  TAccountRng extends string = string,
  TAccountGambaState extends string = string,
> = {
  game: Address<TAccountGame>;
  rng: TransactionSigner<TAccountRng>;
  gambaState?: Address<TAccountGambaState>;
  nextRngSeedHashed: RngProvideHashedSeedInstructionDataArgs['nextRngSeedHashed'];
};

export async function getRngProvideHashedSeedInstructionAsync<
  TAccountGame extends string,
  TAccountRng extends string,
  TAccountGambaState extends string,
  TProgramAddress extends Address = typeof GAMBA_PROGRAM_ADDRESS,
>(
  input: RngProvideHashedSeedAsyncInput<
    TAccountGame,
    TAccountRng,
    TAccountGambaState
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  RngProvideHashedSeedInstruction<
    TProgramAddress,
    TAccountGame,
    TAccountRng,
    TAccountGambaState
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? GAMBA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    game: { value: input.game ?? null, isWritable: true },
    rng: { value: input.rng ?? null, isWritable: true },
    gambaState: { value: input.gambaState ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.gambaState.value) {
    accounts.gambaState.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([34, 71, 65, 77, 66, 65, 95, 83, 84, 65, 84, 69, 34])
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.game),
      getAccountMeta(accounts.rng),
      getAccountMeta(accounts.gambaState),
    ],
    programAddress,
    data: getRngProvideHashedSeedInstructionDataEncoder().encode(
      args as RngProvideHashedSeedInstructionDataArgs
    ),
  } as RngProvideHashedSeedInstruction<
    TProgramAddress,
    TAccountGame,
    TAccountRng,
    TAccountGambaState
  >;

  return instruction;
}

export type RngProvideHashedSeedInput<
  TAccountGame extends string = string,
  TAccountRng extends string = string,
  TAccountGambaState extends string = string,
> = {
  game: Address<TAccountGame>;
  rng: TransactionSigner<TAccountRng>;
  gambaState: Address<TAccountGambaState>;
  nextRngSeedHashed: RngProvideHashedSeedInstructionDataArgs['nextRngSeedHashed'];
};

export function getRngProvideHashedSeedInstruction<
  TAccountGame extends string,
  TAccountRng extends string,
  TAccountGambaState extends string,
  TProgramAddress extends Address = typeof GAMBA_PROGRAM_ADDRESS,
>(
  input: RngProvideHashedSeedInput<
    TAccountGame,
    TAccountRng,
    TAccountGambaState
  >,
  config?: { programAddress?: TProgramAddress }
): RngProvideHashedSeedInstruction<
  TProgramAddress,
  TAccountGame,
  TAccountRng,
  TAccountGambaState
> {
  // Program address.
  const programAddress = config?.programAddress ?? GAMBA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    game: { value: input.game ?? null, isWritable: true },
    rng: { value: input.rng ?? null, isWritable: true },
    gambaState: { value: input.gambaState ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.game),
      getAccountMeta(accounts.rng),
      getAccountMeta(accounts.gambaState),
    ],
    programAddress,
    data: getRngProvideHashedSeedInstructionDataEncoder().encode(
      args as RngProvideHashedSeedInstructionDataArgs
    ),
  } as RngProvideHashedSeedInstruction<
    TProgramAddress,
    TAccountGame,
    TAccountRng,
    TAccountGambaState
  >;

  return instruction;
}

export type ParsedRngProvideHashedSeedInstruction<
  TProgram extends string = typeof GAMBA_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    game: TAccountMetas[0];
    rng: TAccountMetas[1];
    gambaState: TAccountMetas[2];
  };
  data: RngProvideHashedSeedInstructionData;
};

export function parseRngProvideHashedSeedInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedRngProvideHashedSeedInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      game: getNextAccount(),
      rng: getNextAccount(),
      gambaState: getNextAccount(),
    },
    data: getRngProvideHashedSeedInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
